{
  "name": "dongtai-sub",
  "version": "1.0.0",
  "scripts": {
    "build": "tsdown --config ./tsdown.config.ts",
    "begin":"node ./dist/index.mjs",
    "dev": "pnpm build && pnpm begin"
  },
  "main": "./src/index.ts",
  "devDependencies": {
    "@types/jsdom": "^27.0.0",
    "@types/node": "latest",
    "tsdown": "^0.20.3"
  },
  "dependencies": {
    "jsdom": "^28.0.0"
  }
}